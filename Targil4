using System;

namespace Targil1
{
    class Program
    {
        static void Main(string[] args)
        {
            int num,avg ,  sum = 0, Count = 0;
            for (int i = 0; i <= 19; i++)
            {
                Console.WriteLine("Enter  Number of students " + i);
                num = int.Parse(Console.ReadLine());
                
                sum = sum + num;
                
                {
                    

                }
            }
                avg = sum / 20;
                Console.WriteLine(" ");
                Console.WriteLine("The average of the students is " + avg);
                
                

            }
        }
    }

